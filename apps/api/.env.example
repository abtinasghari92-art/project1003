# Copy from repo root `.env.example` or this file.
# Prisma reads `apps/api/.env` first.
#
# Required for admin CRM seed/login:
# ADMIN_EMAIL, ADMIN_PASSWORD, ADMIN_NAME
# CORS: include ADMIN_WEB_URL (http://localhost:3002)
# SHIPPING_RIAL=70000
